<%- include('include/header.ejs') %>

<body>
    <%- include('include/navigation.ejs') %>
    <main>
        <h1>Users</h1>
        <div>
        <% users.forEach((user) => {%>
            <div>
                <h4>Name: <%= user.name %></h4>
            </div>
        <% });%>
            <div class="userArea">
                <span>Logged in user: </span>
                <span><%= userName ? userName : 'No logged in user' %></span>
            </div>
        </div>
    </main>

<%- include('include/footer.ejs') %>
